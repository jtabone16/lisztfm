'use strict';

// Configuring the Playlists module
angular.module('playlists', ['spotify']).run(function( ){
  //Tests for alertify
  // Alertify.success('Successfully submitted activity!');
  // Alertify.error('Activity not submitted...contact support!');
	}
);
